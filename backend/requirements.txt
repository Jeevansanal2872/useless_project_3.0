# FastAPI and ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0          # Async PostgreSQL driver
alembic==1.13.1          # DB migrations

# Redis
redis[hiredis]==5.0.4    # Async Redis client (aioredis merged into redis-py >= 4.2)

# Data validation / serialization
pydantic==2.7.1
pydantic-settings==2.2.1 # Settings management via env vars

# HTTP client (for Piston API calls)
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0  # JWT (optional auth layer)
passlib[bcrypt]==1.7.4            # Password hashing

# Testing (optional but included)
pytest==8.2.0
pytest-asyncio==0.23.7
